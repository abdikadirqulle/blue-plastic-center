# Phase 4 uses the live REST API. Mock repositories remain test fixtures only.
VITE_DATA_SOURCE=api
# Local default is http://<current-host>:4000. Set this for deployed environments.
VITE_API_URL=http://localhost:4000
